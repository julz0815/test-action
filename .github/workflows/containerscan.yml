on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    

jobs:

  container-scan:
      runs-on: ubuntu-latest
      name: container scan

      steps:
        - name: checkout
          uses: actions/checkout@v2

        - name: container-scan action step
          id: container-scan
          uses: veracode/veracode/veracode-container-scanning-action@changeInstallFolder
          with:
            vid: ${{ secrets.VID }}
            vkey: ${{ secrets.VKEY }}
            command: "scan" 
            type: "directory"
            source: "./"
            format: "table"
            debug: true
            fail_build: true
